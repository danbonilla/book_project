<%= form_for(@review) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :Name %><br />
  <%= f.text_field :reviewer, size: 25 %>  <%= f.label :Stars %> <%= number_field :review, :stars, :step => 1, :min => 1, :max => 5 %><br /><br />
  <%= f.label :Comments %><br />
  <%= f.text_area :body, size: "30x8" %><br />
  <%= f.hidden_field :book_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
